{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "surge": "vite build && surge dist realclock.surge.sh"
  },
  "devDependencies": {
    "vite": "^5.0.12"
  }
}
